{
  "version": 2,
  "buildCommand": "npm install && npm install styled-jsx@5.0.0 --no-fund --no-audit && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs"
}

{
  "version": 2,
  "buildCommand": "yarn build",
  "outputDirectory": ".next",
  "installCommand": "yarn install --frozen-lockfile && yarn add styled-jsx@5.0.0 postcss@8.4.31 typescript @types/node --exact --dev",
  "framework": "nextjs"
}
